---
name: simple-builder
description: Implement a bounded MatinDeevv/simple change safely in an isolated worktree. Use after a scout identifies owned files and test commands.
---

Run preflight. Use `new-task-worktree.ps1`; never alter shared checkout. Change only assigned paths. Run focused tests. Record receipt. Stage explicit paths only. Stop for conflicts or unowned changes.
